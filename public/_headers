# Serve the sample-call recordings with the correct audio MIME type so the
# <audio> element on the agent cards can play them on Netlify, and cache
# them long-term (they are content-versioned by filename).
/recordings/*
  Content-Type: audio/wav
  Cache-Control: public, max-age=31536000, immutable
